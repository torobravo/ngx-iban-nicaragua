import { IbanNicaraguaPipe } from './iban-nicaragua.pipe';

describe('IbanNicaraguaPipe', () => {
  it('create an instance', () => {
    const pipe = new IbanNicaraguaPipe();
    expect(pipe).toBeTruthy();
  });
});
