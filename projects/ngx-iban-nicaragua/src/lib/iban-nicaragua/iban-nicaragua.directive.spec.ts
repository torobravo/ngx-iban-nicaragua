import { IbanNicaraguaDirective } from './iban-nicaragua.directive';

describe('IbanNicaraguaDirective', () => {
  it('should create an instance', () => {
    const directive = new IbanNicaraguaDirective();
    expect(directive).toBeTruthy();
  });
});
